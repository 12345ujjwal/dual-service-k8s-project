kind: PersistentVolumeClaim
apiVersion: v1

metadata:
 namespace: dev
 name: local-pvc

spec:
 accessModes:
 - ReadWriteOnce
 resources:
  requests:
   storage: 400Mi
 storageClassName: local-storage
